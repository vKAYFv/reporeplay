{
  "name": "kayf-profile-counter",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "dev": "pnpm --filter @kayf/profile-counter-server dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f app",
    "docker:reset": "docker compose down -v",
    "loadtest:100k": "docker compose --profile loadtest run --rm -e SCENARIO=100k loadtest",
    "loadtest:burst": "docker compose --profile loadtest run --rm -e SCENARIO=burst loadtest",
    "loadtest:unique": "docker compose --profile loadtest run --rm -e SCENARIO=unique loadtest",
    "loadtest:repeat": "docker compose --profile loadtest run --rm -e SCENARIO=repeat loadtest",
    "test:integration": "docker compose --profile test run --rm test"
  }
}
